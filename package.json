{
    "name": "workspace",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "node build.js",
        "watch": "nodemon --delay 500ms --watch src --watch build.js --ext js --ignore dist --ignore node_modules --exec \"npm run build\"",
        "serve": "browser-sync start --server dist --files 'dist/**/*' --no-notify --no-open",
        "dev": "concurrently \"npm run watch\" \"npm run serve\""
    }
}
